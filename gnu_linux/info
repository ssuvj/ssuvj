[autostart(openbox)] configurations for openbox, tint2 and more

[info] a minimalist instalation, trying to make all in one command for future use

[coment] i break something always

[info] os: debian

[info] version: current (debian 13 'trixie')

[info((info))] information itself

[instalation.bash] packages i need for, later on trying to put just one command to put {
    create and put "[autostart(openbox)]"
    create and put "[rc.xml]"
    create and put "[tint2.rc]"
}

[info] no graphic login